Yêu cầu:

- Shopping cart:
  - Phải get được data từ folder mocking-data/cart-data.json để đổ lên giao diện (cartItems)
  - Có thể thêm số lượng sản phẩm và tăng số tiền tương ứng
  - Có thể xóa sản phẩm
- Card details:
  - Phải get được data từ folder mocking-data/cart-data.json để đổ lên giao diện (cardDetails)
  - Khi chỉnh sửa data ở phía dưới phải update real-time lên trên card
  - Khi bấm check out phải console log ra toàn bộ data theo định dạng như file cart-data.json

* Không sử dụng thư viện, không add thêm bất cứ thư viện nào vào trong ứng app
* Giao diện phải giống hoàn toàn như trong design
* Sử dụng font chữ như trong source đã tạo sẵn, không cần phải đổi font chữ khác